<UserControl x:Class="ERModsManager.UCs.LogsUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ERModsManager.UCs"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400" Background="{DynamicResource WindowBrush}" BorderBrush="#33FFFFFF" BorderThickness="0,1,0,1">
    <Grid Margin="10">
        <Label Foreground="{DynamicResource WindowTextBrush}" FontSize="17" FontWeight="Bold" Margin="0,0,0,20" FontFamily="Liberation Sans" VerticalAlignment="Top">Logs</Label>
        <ScrollViewer x:Name="LogScrollViewer" Margin="0,42,0,0" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled">
            <Grid>
                <TextBlock Visibility="Hidden"  Foreground="{DynamicResource WindowTextBrush}" x:Name="TxtLogs" FontSize="12" FontFamily="Liberation Sans"></TextBlock>

                <StackPanel x:Name="LogsStackPanel" Orientation="Vertical">
                    
                </StackPanel>
                
            </Grid>
        </ScrollViewer>
        
        
       
    </Grid>
</UserControl>
